<% @items.each do |item| %>
  <p>
    <%= item.title %> | $<%= item.price.to_f/100 %> |
    <% if current_user %>
      <%= button_to 'Add to Cart', line_items_path(item_id: item) %>
    <% end %>
  </p>
<% end %>
